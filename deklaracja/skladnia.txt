Tutaj pokrótce przedstawiam opis języka:

// na 15 punktów:
1. Typy: int, bool, string, fun (funkcja), anonfun
2, 3, 4 - standardowo
5. while - standardowo, if bez else: if (x > 0) { x = x + 1 }
   if z else: ifelse (x > 0) { print("plus") } { print("nieplus") }
6. 7. funkcje z przekazaniem przez wartosc: fun int odpowiedz(string pytanie) {return 42}
      przekazanie przez zmienną: fun string odpowiedz(var string pytanie) {pytanie = "42"; return "odpowiedz jest w pytaniu"}

// na 20 punktów:
9. standardowo
10. komunikat i zatrzymanie interpretera 
11. zgodnie z 6,7, bez procedur

// na 30 punktów:
12, 13 - wiadomo o co chodzi
17. funkcja anonimowa: lambda x: 42+x
    będzie s
